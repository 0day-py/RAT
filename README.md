# RAT
A remote access tool using discord as a c2 server, designed to be small and minimal to gain persistence and shell access. Use responsibly against authorized targets only.

How to use: Put your bot token and webhook in the required fields, recommended to obfuscate it, then download and run the payload with your bot token and webhook on the target device.
